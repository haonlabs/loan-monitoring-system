import type { Payment } from "@/types";
import { formatDate, formatRupiah } from "@/lib/utils";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
export function PaymentHistory({payments,actions}:{payments:Payment[];actions?:(p:Payment)=>React.ReactNode}){if(!payments.length)return <p className="py-8 text-center text-sm text-muted-foreground">Belum ada pembayaran.</p>;return <Table><TableHeader><TableRow><TableHead>Tanggal</TableHead><TableHead>Jumlah bayar</TableHead><TableHead>Sisa setelah bayar</TableHead><TableHead>Catatan</TableHead>{actions&&<TableHead/>}</TableRow></TableHeader><TableBody>{payments.map(p=><TableRow key={p.id}><TableCell>{formatDate(p.paidAt)}</TableCell><TableCell className="font-semibold text-emerald-700">{formatRupiah(p.amount)}</TableCell><TableCell>{formatRupiah(p.remainingAfter)}</TableCell><TableCell className="text-muted-foreground">{p.note||"-"}</TableCell>{actions&&<TableCell className="text-right">{actions(p)}</TableCell>}</TableRow>)}</TableBody></Table>}
